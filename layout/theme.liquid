<!doctype html>
<html lang='{{ request.locale.iso_code }}'>
  <head>
    {% # Load Halyard Display font %}
    <link rel='stylesheet' href='https://use.typekit.net/pgu6zyr.css'>

    {% # Inlined CSS Variables %}
    {% render "css-variables" %}

    {% # Load and preload the critical CSS %}
    {{ "critical.css" | asset_url | stylesheet_tag: preload: true }}

    {% # Load Tailwind CSS %}
    {{ "tailwind.css" | asset_url | stylesheet_tag }}

    {% # Load Lenis smooth scrolling %}
    <script
      src='https://unpkg.com/@studio-freight/lenis@1.0.19/dist/lenis.min.js'
      defer
    ></script>

    {% # Social, title, etc. %}
    {% render "meta-tags" %}

    {% # Hide Shopify cookie consent banner %}
    <style>
      #shopify-pc__banner,
      .shopify-pc__banner__dialog,
      .shopify-pc__banner__wrapper {
        display: none !important;
        visibility: hidden !important;
        opacity: 0 !important;
        pointer-events: none !important;
      }
    </style>

    {{ content_for_header }}
  </head>

  <body>
    {% comment %} {% sections 'header-group' %} {% endcomment %}

    {{ content_for_layout }}

    {% comment %} {% sections "footer-group" %} {% endcomment %}

    {% # Initialize Lenis smooth scroll %}
    <script src='{{ 'lenis-scroll.js' | asset_url }}' defer></script>
  </body>
</html>
